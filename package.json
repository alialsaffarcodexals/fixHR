{
  "name": "enterprise-payroll-suite",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "pnpm -C apps/api dev & pnpm -C apps/web dev",
    "build": "pnpm -C apps/api build && pnpm -C apps/web build",
    "test": "pnpm -C apps/api test && pnpm -C apps/web test",
    "coverage": "pnpm -C apps/api coverage && pnpm -C apps/web coverage"
  }
}